/** Host loader entry for the Desktop packager browser implementation exported from `./client`. */

/** Host plugin body — the packager service lives in `@deepseek-ai/dsh-experimental-desktop-packager`. */
export function apply(): void {}
